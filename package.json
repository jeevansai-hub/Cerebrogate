{
  "name": "cerebrogate-root",
  "version": "1.0.0",
  "description": "Root configuration for deploying CerebroGate",
  "scripts": {
    "install-backend": "npm install --prefix backend",
    "build": "npm run install-backend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
